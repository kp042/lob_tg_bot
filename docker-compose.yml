services:
  lob-tg-bot:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: lob_tg_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - app_images:/tmp/images
    environment:
      - PYTHONPATH=/app
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  app_images:
